package com.nitish.compliance.pack;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class RequirementTypeTest {
/*
    @Test
    void shouldContainExpectedRequirementTypes() {

        assertEquals(
                RequirementType.LAW,
                RequirementType.valueOf("LAW")
        );

        assertEquals(
                RequirementType.REGULATION,
                RequirementType.valueOf("REGULATION")
        );

        assertEquals(
                RequirementType.STANDARD,
                RequirementType.valueOf("STANDARD")
        );

        assertEquals(
                RequirementType.AUTHORISATION,
                RequirementType.valueOf("AUTHORISATION")
        );

        assertEquals(
                RequirementType.CUSTOMER_REQUIREMENT,
                RequirementType.valueOf("CUSTOMER_REQUIREMENT")
        );

        assertEquals(
                RequirementType.TECHNICAL_REQUIREMENT,
                RequirementType.valueOf("TECHNICAL_REQUIREMENT")
        );

        assertEquals(
                RequirementType.PROCESS_RISK,
                RequirementType.valueOf("PROCESS_RISK")
        );

    }

    @Test
    void shouldContainExactlySevenRequirementTypes() {

        assertEquals(
                7,
                RequirementType.values().length
        );
    }

    @Test
    void shouldContainExactlyThreeStatuses() {

        assertEquals(
                3,
                RequirementPackStatus.values().length
        );
    }*/
}