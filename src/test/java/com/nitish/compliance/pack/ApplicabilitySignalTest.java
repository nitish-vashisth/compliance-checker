package com.nitish.compliance.pack;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ApplicabilitySignalTest {

    /*
    @Test
    void shouldCreateSignal() {
        ApplicabilitySignal signal =
                new ApplicabilitySignal(
                        "data.personal_data",
                        "Personal data processed",
                        "Whether the organisation processes personal data",
                        SignalDataType.BOOLEAN,
                        true
                );

        assertEquals("data.personal_data", signal.id());
        assertEquals("Personal data processed", signal.name());
        assertEquals(
                "Whether the organisation processes personal data",
                signal.description()
        );
        assertEquals("BOOLEAN", SignalDataType.BOOLEAN.name());
        assertTrue(signal.required());
    }

    @Test
    void shouldRejectBlankId() {
        assertThrows(
                IllegalArgumentException.class,
                () -> new ApplicabilitySignal(
                        "",
                        "Personal data processed",
                        "Description",
                        SignalDataType.BOOLEAN,
                        true
                )
        );
    }

    @Test
    void shouldRejectBlankName() {
        assertThrows(
                IllegalArgumentException.class,
                () -> new ApplicabilitySignal(
                        "data.personal_data",
                        "",
                        "Description",
                        SignalDataType.BOOLEAN,
                        true
                )
        );
    }

    @Test
    void shouldRejectBlankDescription() {
        assertThrows(
                IllegalArgumentException.class,
                () -> new ApplicabilitySignal(
                        "data.personal_data",
                        "Personal data processed",
                        "",
                        SignalDataType.BOOLEAN,
                        true
                )
        );
    }*/

}